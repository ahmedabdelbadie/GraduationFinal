<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aboabdel_badie.graduationfinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" />

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Start.Start.Luncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Start.Start.MainActivity" />
        <activity android:name=".Start.Loginp.Login" />
        <activity android:name=".Start.Regp.Id" />
        <activity android:name=".Start.Regp.Reg" />
        <activity android:name=".Start.Master.Main" />
        <activity android:name=".Start.Masterp.Health" />
        <activity android:name=".Start.Masterp.Treat" />
        <activity android:name=".Start.Masterp.Food" />
        <activity android:name=".Start.Masterp.NewPhone" />
        <activity android:name=".Start.Masterp.Contact" />

        <receiver android:name=".Start.Master.AlarmReceiver">
            <intent-filter>
                <action android:name="android.media.action.DISPLAY_NOTIFICATION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <activity android:name=".Start.Masterp.Sport"></activity>
    </application>

</manifest>